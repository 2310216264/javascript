同源策略
    1、同协议
    2、同域名/同ip
    3、同端口号

【注】ajax只能下载同源的数据，跨源数据，跨源的数据禁止访问

跨源的需求：

跨源方式：
    1、修改ajax同源协议(不建议)
    2、委托PHP文件进行跨源
    3、JSONP

JSOP跨域的使用流程
    1、先去声明一个函数，这个函数有一个形参。会拿到想要获取的数
    2、在需要获取数据的时候，动态创建script标签，将标签src属性设置成，数据的链接
    3、当script插入到页面上的时候，就会。调用已经封装好的函数，将我们需要的数据传过来


cookie
    1、可以设置过期时间
    2、最大可以存储4kb，每一个域名下最多可以存储50条数据(不同的浏览器)
【注】只能存储字符串
cookie语法
格式：name=value;expires=date;path=path;domain=somewhere.com;secure
name  键
value 值
除name和value 其他都是可选项

expires:过期时间
    必须填写，日期对象
    【注】系统会自动清除过期的cookie

    path限制访问路径
        如果不去设置，默认是加载当前HTML文件的路径
    【注】我们设置的cookie的路径，和加载当前文件的路径必须入职，如果不一致，则会访问失败

    domain 限制访问域名
        如果不去设置，默认是加载当前HTML文件服务器的域名/IP
    [注]如果加载当前文件域名和设置域名不一致，设置cookie失败

    secure
        如果不设置，cookie可以通过http或https协议加载文件设置
        设置字段以后，只能通过https协议加载cookie
https证书认证协议
http 